/*

class ClipperThree extends CustomClipper<Path>{

  Path getClip(Size size){

    Path path = Path();
    path.moveTo(100, 100);
    path.lineTo(0, size.height);
    path.lineTo(size.height, size.height);
    return path;

  }

  bool shouldReclip(covariant CustomClipper<Path> oldClippper){

    return true;

  }
}
 */